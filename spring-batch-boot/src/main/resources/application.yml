spring:
  application:
    name: spring-batch-boot
  batch:
    job:
      name: terminatorJob
#      name: processTerminatorJob
#      name: deleteOldFilesJob
#      name: systemTerminationSimulationJob
#      name: zombieCleanupJob

